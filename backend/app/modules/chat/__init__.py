# See PROJECT_PLAN.md §8 for exact method signatures and behaviour.
# router.py  → FastAPI router, mounts at /api/v1/chat
# service.py → ChatService class
# schemas.py → ChatSession, ChatMessage, ChatRequest, Source, RAGEvent

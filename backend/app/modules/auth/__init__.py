# See PROJECT_PLAN.md §8 for exact method signatures and behaviour.
# router.py  → FastAPI router, mounts at /api/v1/auth
# service.py → AuthService class
# schemas.py → Pydantic models: LoginRequest, TokenResponse, UserResponse

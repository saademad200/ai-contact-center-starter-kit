name: Deploy Frontend to ECS

on:
  push:
    branches:
      - main          # production — protected, merge-only
      - release/rc    # staging / release candidate — protected, merge-only
    paths:
      - 'frontend/**'
      - 'infrastructure/codedeploy/appspec-frontend.yaml'
      - 'infrastructure/codedeploy/taskdef-frontend.json'
      - '.github/workflows/deploy-frontend.yml'

env:
  AWS_REGION: us-east-1

jobs:
  deploy:
    name: Deploy Frontend → ${{ github.ref_name == 'main' && 'Production' || 'Staging' }}
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name == 'main' && 'production' || 'staging' }}
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve deployment target
        id: target
        run: |
          if [ "${{ github.ref_name }}" = "main" ]; then
            echo "env_name=prod"                                         >> $GITHUB_OUTPUT
            echo "ecs_cluster=alfalah-ai-prod-cluster"                  >> $GITHUB_OUTPUT
            echo "ecs_service=alfalah-ai-prod-front-svc"                >> $GITHUB_OUTPUT
            echo "ecr_repo=alfalah-ai-prod-frontend"                    >> $GITHUB_OUTPUT
          else
            echo "env_name=staging"                                      >> $GITHUB_OUTPUT
            echo "ecs_cluster=alfalah-ai-staging-cluster"               >> $GITHUB_OUTPUT
            echo "ecs_service=alfalah-ai-staging-front-svc"             >> $GITHUB_OUTPUT
            echo "ecr_repo=alfalah-ai-staging-frontend"                 >> $GITHUB_OUTPUT
          fi

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push Frontend image
        id: build-image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: ${{ steps.target.outputs.ecr_repo }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG -f frontend/Dockerfile ./frontend
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT

      - name: Render task definition
        id: task-def
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: infrastructure/codedeploy/taskdef-frontend.json
          container-name: front
          image: ${{ steps.build-image.outputs.image }}

      - name: Deploy to Amazon ECS (Rolling Update)
        uses: aws-actions/amazon-ecs-deploy-task-definition@v1
        with:
          task-definition: ${{ steps.task-def.outputs.task-definition }}
          service: ${{ steps.target.outputs.ecs_service }}
          cluster: ${{ steps.target.outputs.ecs_cluster }}
          wait-for-service-stability: true
          force-new-deployment: true
